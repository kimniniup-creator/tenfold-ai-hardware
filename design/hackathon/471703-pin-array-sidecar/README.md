# 十日环｜针板式 EDC · M5 侧舱适配原型

**状态：已建模的独立配件，待原针板尺寸校准；不是完整复刻，也未实物打印。**

核心体验仍由 pearigee 的「大型手持针板解压玩具」提供：手指和掌面压动密集滑销，翻转后再压，形成连续触感。侧舱只承载 M5StickS3，不替代针阵、不封住针板背面、不把滑销改成屏幕按钮。原作品必须自行从作者页面合法取得，本目录不包含原针板或其衍生几何。

## 已核实与未核实

| 项目 | 证据与结论 |
|---|---|
| 原模型 | [MakerWorld 中国站 471703](https://makerworld.com.cn/models/471703)，名称「大型手持针板解压玩具」，作者 pearigee；2026-09-22 实际浏览器核对 |
| 机构 | 页面说明、照片及标签确认一体打印滑销针板；作者提示首层公差小，需释放滑销。销钉数量、直径、行程、限位形状均未测得 |
| 下载项 | 绿色按钮右侧下拉 → 下载 STL/CAD 文件 → `Pin art board medium.stl`，52 MB；点击下载提示登录。另有 3MF 和 5 个打印配置 |
| 许可 | 页面 Standard Digital File License 明确限制原文件及衍生作品再分发。这里只提供独立原创夹具/侧舱，不提供复制针阵 |
| 原针板尺寸 | **未知。不得从封面照片推算后宣称实测** |
| M5 | [官方文档](https://docs.m5stack.com/en/core/StickS3)：整机 48×24×15 mm；[尺寸图](https://m5stack-doc.oss-cn-shenzhen.aliyuncs.com/1207/K150-sticks3.pdf)；[结构文件入口](https://github.com/m5stack/M5_Hardware/tree/master/Products/K150_StickS3/Structures) |
| M5 官方 STL | 已下载供本地分析，不再分发。文件整体 X 跨度 138.529 mm，显然不能直接当整机包络；需按分离部件辨认，模型使用官方标称整机包络 |
| 制造与体验 | 未实物打印、未验证手感、未跑耐久或跌落；未验证原针板接口与运动干涉 |

## 结构与机械保留

侧舱主体 64×40 mm，附 10 mm 安装翼；M5 位置为 X=8…56、Y=8…32、Z=3…18 mm。四角约束给名义单侧 0.5 mm 横向余量；盖框底面 Z=18.3 mm，前面名义间隙 0.3 mm。背面放 0.5 mm EVA 薄垫，四角盖唇约束抬起方向，整机无需拆壳。

两组夹具位于 Y=9、31 mm，沿针板**静态外边框**固定。针板在 X≤-12 一侧，电子舱在 X≥0 一侧。夹具只伸入静态框 LAND mm，不得伸到滑销区。参数默认 `frame_thickness=8`、`frame_land=6` 是**工程校准样例**，不是原模型尺寸。当前 22 mm 夹具中心距、10 mm 夹具宽度也需结合真实外框/把手位置复核。

不使用整面背板、不在针阵下面堆电子件，保留压入、推出、翻转的机械自由度。加装后重心、握持和外缘可达性仍需实物评估。若原模型没有≥4 mm 宽且可夹持的静态边框，此夹持方案不成立，须依据实际把手设计替换连接件，不能夹滑销凑合。

## 文件与复现

- `build.py`：参数化 CadQuery 实体源，所有几何从尺寸和布尔运算生成。
- `exports/*.step`：四个独立零件和独立配件装配 STEP。
- `exports/*.stl`：四种打印件，底面已归零；上下夹具各打印两件。
- `exports/assembly.png`、`exploded.png`：真实 CAD 三角化装配及爆炸预览。橙色是 M5 标称包络，不是原针板或精细硬件复刻。
- `exports/validation.json`：网格闭合、包围盒、体积、实体相交检查。

```powershell
python -m venv .venv
.venv/Scripts/python -m pip install -r requirements.txt
.venv/Scripts/python build.py --frame-thickness 8 --frame-land 6
```

导出脚本会覆盖 `exports`；正式实测参数调整应先改 HANDOFF 记录再生成。脚本约束厚度4…14 mm、静态夹持宽4…10 mm，只表示此夹具的参数设计范围，不表示原针板适配范围。

## BOM 与打印

| 项目 | 数量 | 要求 |
|---|---:|---|
| M5StickS3 K150 | 1 | 保持原壳完整 |
| sidecar_base / corner_cover | 各1 | PETG，0.2 mm 层高，4墙，30%填充；盖框平放 |
| lower_clamp / upper_clamp | 各2 | PETG，0.2 mm，5墙，40%填充；底面平放 |
| M2.5×8 自攻型塑料螺钉 | 4 | 2.4 mm 导孔为试配值；先用同材料试孔，不匹配则调整 |
| M3 螺栓、螺母、垫片 | 各4 | 8 mm框样例选M3×20；按最终堆叠+螺母+至少两牙余量复核 |
| EVA/硅胶垫 0.5 mm | 若干 | 框接触面及 M5 背部；不得遮住端口或散热/音孔 |
| 原针板 | 1 | 作者原文件自备，保持原机构；遵守作者许可 |

打印前先做小段孔径与夹持公差样件；此版本不以摩擦估计值宣称跌落可靠。基座柱/导向壁为悬臂，PETG尺寸与螺钉扭矩需实物确认。打印件去毛刺后再装电子件。原针板按作者配置单独打印、释放滑销，不在装上电子件后敲击针板。

## 装配与避让检查

1. 先确保原针板独立正常工作；测量外框厚度、可夹宽、直线可用长度、滑销正反面极限与把手轮廓。
2. 修改参数并导出。夹爪内侧铺0.5 mm软垫，下夹具位于安装翼下方；安装翼和下夹具间0.3 mm间隙用薄垫调平。螺栓依次穿过下夹具、安装翼、上夹具，外侧装垫片/螺母，均匀轻拧。
3. 放入带0.5 mm背垫的M5，装四角盖框和四枚螺钉；必须确认前面盖唇未压屏幕/按键，不能以拧紧强行消除尺寸不符。
4. 两长侧中央 X=12…52、短侧中央 Y=12…28 为主要开放窗口。USB-C插头、侧Reset、两个可编程键、Hat2、Grove必须逐一插拔/按压检查。几何上侧面大范围开放，但接口精确坐标和所用插头外壳未实测，**不宣称已经完成接口操作验证**。若某端口落在角柱遮挡区，调整柱位后重跑检查。
5. 对针板每枚滑销做全行程，正反翻转，检查夹具/螺栓/电缆未触及滑销；未取到原STL前此项为阻塞项。夹持后不应让外框变形压紧销钉。

## 十日环交互映射（设计，未实现固件）

| 动作 | 语义 | 首版技术边界 |
|---|---|---|
| 随意压动针阵、掌面来回扫 | 回到当下，保留机械减压 | 完全机械、无需电；不自动增加完成计数 |
| 拿起/翻转装置 | 唤醒当日状态 | 可用M5 BMI270 IMU；抖动门限与误触需固件测试 |
| 按可编程键确认一次 | 完成今天，屏幕十格前进一格 | 十珠降级为屏幕十格；每日幂等、防误触确认 |
| 长按另一个可编程键 | Seal 停止追加 | 禁用系统Reset键作为业务操作 |
| 双击可编程键 | 24小时恢复窗口 | 需要UI反馈；不能由随机针阵运动触发 |
| 十天完成后双键确认 | 新周期复位 | 保留历史，不自动清零 |

针阵是自由把玩机构，无逐日机械锁存，不伪称十珠或单销位置检测。相比原PRD，携带体积和十珠状态表达明确降级，机械把玩功能优先保留。

## 下一道验收门

取得合法原STL → 本地测量边框和滑销行程 → 按实测参数重导出夹具 → 原件+夹具全行程干涉检查 → PETG试打印 → M5装机按键/USB检查 → 10分钟把玩及50次翻转 → 再决定是否进入黑客松演示。当前只能验收独立CAD与其名义包络检查，不能验收针板整机适配成功。

夹爪补充：上夹具在自由位置向下最多0.4 mm到达边缘硬限位，使两侧0.5 mm软垫总压缩量不超过0.4 mm；需实物确认垫材硬度和框受力。该行程已纳入脚本分段干涉检查，不能代替原针阵运动验证。
