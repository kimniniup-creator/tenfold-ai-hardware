# 今晚软件合流与差异表

2026-09-22；只读软件任务未提交的MainActivity.java、UsbTransport相关说明和docs/android-demo.md所得快照，不是最终软件验收。软件任务：01a0c5c6-3e14-7e80-b9c0-3e7159c57977，独占D:/tenfold-worktrees/android；产品不改其文件。

保留工程选定的Android Java、USB Host→M5 USB CDC换行JSON、模拟适配器与本地规则，不要求另做BLE/Wi-Fi。不依赖新界面大改才能先产APK。当前未有板卡实测，USB和固件源不等于端到端通过。

| 项目 | 已看到的基础 | 本轮最小改动/验收 | 优先级 |
|---|---|---|---|
| 首页 | 有今日动作、当前周期 | 已有承诺直接今日卡与“今天到这里”；首次设置才问十日结果/卡点/预算 | P0 |
| Agent | 明确OFFLINE RULES，无LLM调用 | 保留真实标识；真实LLM为单独未完成项，不能以规则冒充。Agent提出停止边界/恢复线索，用户确认 | P0真伪；LLM按可用接入推进 |
| 封存 | 有seal事件 | 可选停在哪一句；先存当前卡，再提议恢复卡；云失败保留已有事实，未完成不改done | P0 |
| 恢复 | 按钮直接改ACTIVE | 展示恢复线索再确认；明确日期与昨日done；已完成须新动作 | P0 |
| 模式 | 依赖phase前缀显示模拟 | 独立real/simulated字段，seal/resume/重启不丢标签，演示与真机历史不混 | P0 |
| ACK | 有发送入口与待收取显示 | 发送只代表pending；phone_saved/device_saved分开；超时能重试同command_id | P0 |
| 日期 | 静态1/10 | 独立DEMO TIME会话按钮推进一日，走同一恢复状态逻辑；生产日期固定时区锚点，未知时间不自动推进 | P0 |
| APK | 构建打包验证进行中 | 签名/包信息/安装启动/冷启动保存；无真机可用模拟器验证并明确不是手机实测 | P0 |
| 完整协议 | 当前offer/event字段少于产品v1 | 软件交付列已实现/缺失字段；不称完整v1通过；优先稳定ID/持久化/恢复 | P0诚实边界 |

上述差异已发送软件任务和协调。该表是工程动作清单而非声称对方已承诺或完成；以软件最终SHA/实测报告关闭各项。

演示时间策略：正常模式使用用户确认的固定时区日期与cycle_start_date。演示模式独立数据，常驻DEMO TIME；“演示下一天”将demo_date加一天并触发同一跨日转换，既不改手机系统时间，也不修改真实周期历史。昨天done=false时恢复已有卡；done=true时等待确认新动作。不可只改日期文字而绕过状态逻辑。
