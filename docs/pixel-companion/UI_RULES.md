# Pet home screen

- The pet owns the main screen. Interaction and session-marker counts are tiny top-right numbers, not headline rows.
- Retain bottom A/B interaction, marker and long-B mode instructions.
- Do not display “我在，陪你待会儿”, “陪你待会儿”, or substitute generic companion reassurance. Idle home has no filler sentence. Agent copy is owned by its separate user task.
- Hold A+B for 3 seconds, release both, then A confirms pet-only reset; B cancels. The confirmation clears pet interaction and marker history, not M5 firmware or other namespaces.
- This reading-schema1 reset must be migrated alongside the pending task-growth schema2 integration; never ship it as if it resets schema2 task records.
